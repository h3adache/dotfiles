# Darwin specific config

alias ls='ls -G'

export JDK_VERSION=1.8
export JAVA_HOME=$(/usr/libexec/java_home -v $JDK_VERSION)
export IDEA_JDK=$JAVA_HOME
export JRE_HOME=$JAVA_HOME

export PATH="${JAVA_HOME}/bin:${PATH}"
